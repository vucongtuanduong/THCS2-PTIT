#include <stdio.h>
typedef struct word{
	char word[100];
	int dem;
} word;
int check(char s[]) {
	int length = strlen(s);
	for (int i = 0; i < length - 1; i++) {
		
	}
}
int main () {
	word a[1001];
	char s[1001];
	int n = 0;
	while (scanf("%s", s) != -1) {
		
	}
}